const CustomTooltip = ({ tooltip }: any) => {
	6;
	return tooltip ? <div className="relative z-20 rounded-lg bg-black px-4 py-2 text-white">{`${10000} Views`}</div> : null;
};

export default CustomTooltip;
